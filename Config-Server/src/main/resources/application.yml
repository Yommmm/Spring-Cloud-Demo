server:
  port: 7000

spring:
  application:
    name: Config-Server
  
  cloud:
    config:
      server:
        git:
          search-paths:
          - config
          uri: https://github.com/Yommmm/Spring-Cloud-Demo
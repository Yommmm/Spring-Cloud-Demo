server:
  port: 8072
  
spring:
  application:
    name: Spring-Boot-Consul-P
    
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: Spring-Boot-Consul-P